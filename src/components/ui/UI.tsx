"use client";

/**
 * UI — minimal interface layer overlaying the world.
 * Per 05_Visual_Design.md: "The interface should stay minimal and allow
 * the world to remain the primary focus."
 * Implementation begins in Phase 3 (Destinations, Content panels, UI).
 */
export function UI() {
  return null;
}
