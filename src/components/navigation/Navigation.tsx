"use client";

/**
 * Navigation — destination selection UI.
 * Reads from the central destination registry (src/lib/destinations.ts).
 * Travel logic implemented in Phase 2/3.
 */
export function Navigation() {
  return null;
}
